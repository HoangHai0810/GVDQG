"use strict";
! function(e, t) {
    e.heroWidget = function(e, t) {
        let i = this;
        i.element = e, i.tabletBreakpoint = 1023, i.mainWrapper = document.querySelector(".tabbedHome"), i.setRightPos(), i.setListeners()
    }, e.heroWidget.prototype.setListeners = function() {
        let e, t = this;
        window.addEventListener("resize", (function(i) {
            e && window.cancelAnimationFrame(e), e = window.requestAnimationFrame((function() {
                t.setRightPos()
            }))
        }), !1)
    }, e.heroWidget.prototype.setRightPos = function() {
        let e = this;
        e.pageWidth = window.innerWidth, e.mainWrapperWidth = e.mainWrapper.clientWidth;
        let t = e.element.querySelector(".heroArticle__bg-shape");
        if (e.pageWidth > e.tabletBreakpoint) {
            const i = (e.pageWidth - e.mainWrapperWidth) / 2;
            t.style.right = "-" + i + "px"
        } else t.removeAttribute("style")
    };
    let i = document.querySelectorAll('[data-widget="hero"]');
    for (let t = 0; t < i.length; t++) new e.heroWidget(i[t], {})
}(PULSE.app, PULSE.app.common);