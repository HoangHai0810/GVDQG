"use strict";
! function(e, t) {
    e.playerImage = function(e, a) {
        var i = this;
        i.element = e, i.playerId = i.element.getAttribute("data-player"), i.imageSize = i.element.getAttribute("data-size") || "140", i.imgExtension = i.element.getAttribute("data-extension"), t.setPlayerImageLoader(i.playerId, i.imageSize, i.element, i.imgExtension)
    };
    for (var a = document.querySelectorAll('[data-widget="player-image"]'), i = 0; i < a.length; i++) new e.playerImage(a[i], {})
}(PULSE.app, PULSE.app.common);