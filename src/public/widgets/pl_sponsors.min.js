"use strict";
! function(t, e, o, s, n) {
    t.HomeSponsorsPlaylist = function(t) {
        const e = this;
        e.container = t, e.scrollContainer = e.container.querySelector(".js-scroll-container"), e.sponsorItems = e.container.querySelectorAll(".js-item"), e.labelContainer = e.container.querySelector(".js-sponsor-label-container"), e.labelItems = e.container.querySelectorAll(".js-sponsor-label"), e.paginationContainer = e.container.querySelector(".js-pagination-container"), e.paginationItems = e.container.querySelectorAll(".js-pagination-item"), e.totalSponsors = e.container.dataset.sponsorsAmount, e.currentPage = 0, e.intervalSet = !1, e.getDefaultValues(), e.setListeners()
    }, t.HomeSponsorsPlaylist.prototype.getDefaultValues = function() {
        const t = this;
        t.itemsToShow = Math.floor(t.scrollContainer.offsetWidth / 150), t.totalPages = Math.ceil(t.totalSponsors / t.itemsToShow), t.setNumberOfItemsActive(t.itemsToShow), t.showPaginationItems()
    }, t.HomeSponsorsPlaylist.prototype.setListeners = function() {
        const t = this;
        1 === t.totalPages || t.intervalSet || (setInterval((() => {
            t.showNextPage()
        }), 5e3), t.intervalSet = !0, window.addEventListener("resize", t.getDefaultValues.bind(t)))
    }, t.HomeSponsorsPlaylist.prototype.setNumberOfItemsActive = function(t) {
        let e = arguments.length > 1 && void 0 !== arguments[1] ? arguments[1] : 0;
        const o = this,
            s = e + t;
        o.setAllItemsInactive();
        for (let t = e; t < s; t++) {
            const e = o.sponsorItems[t];
            e && e.classList.add("is-active")
        }
    }, t.HomeSponsorsPlaylist.prototype.setAllItemsInactive = function() {
        this.scrollContainer.querySelectorAll(".is-active").forEach((t => {
            t.classList.remove("is-active")
        }))
    }, t.HomeSponsorsPlaylist.prototype.showNextPage = function() {
        const t = this;
        t.currentPage = t.currentPage + 1, t.startPosition = t.currentPage * t.itemsToShow, t.currentPage > t.totalPages - 1 && (t.currentPage = 0, t.startPosition = 0), t.setNumberOfItemsActive(t.itemsToShow, t.startPosition), t.setActivePaginationItem()
    }, t.HomeSponsorsPlaylist.prototype.setActivePaginationItem = function() {
        const t = this,
            e = t.paginationContainer.querySelector(".is-active"),
            o = t.labelContainer.querySelector(".is-active");
        e && (e.classList.remove("is-active"), o.classList.remove("is-active")), t.paginationItems[t.currentPage].classList.add("is-active"), t.labelItems[t.currentPage].classList.add("is-active")
    }, t.HomeSponsorsPlaylist.prototype.showPaginationItems = function() {
        const t = this;
        for (let e = 0; e < t.paginationItems.length; e++) {
            const o = t.paginationItems[e];
            e < t.totalPages ? o.classList.remove("u-hide") : o.classList.add("u-hide")
        }
        t.setActivePaginationItem()
    };
    const i = document.querySelectorAll('[data-widget="home-sponsors-playlist"]');
    Array.prototype.map.call(i, (function(e) {
        new t.HomeSponsorsPlaylist(e, {})
    }))
}(PULSE.app, PULSE.ui, PULSE.app.common, PULSE.core, PULSE.I18N);