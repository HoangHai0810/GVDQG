"use strict";
PULSE.app.templates.horizontal = '{{ #list }} {{ #status }} <li class="article-thumb placeholder" data-initialised="false"> <a href="{{url}}" class="thumbnail"> <figure> <span class="image thumbCrop-latestnews"> <picture class=""> {{ #thumbnailUrlFull }} <img src="{{ thumbnailUrlFull }}" alt="{{ title }}" class="article-thumb__img"> {{ /thumbnailUrlFull }} </picture> </span> <figcaption> <span class="tag">{{ generalTag }}</span> <span class="title">{{ title }}</span> </figcaption> </figure> </a> </li> {{ /status }} {{ /list }}',
    function(t, i, a, l) {
        t.quizLatest = function(t) {
            this.quizList = new a.QuizList(t, "horizontal")
        };
        let e = document.querySelectorAll('[data-widget="quiz-list"]');
        Array.prototype.map.call(e, (function(i) {
            i = new t.quizLatest(i)
        }))
    }(PULSE.app, PULSE.ui, PULSE.app.common, PULSE.core),
    function(t, i, a, l) {
        t.videoList = function(t) {
            this.videoList = new a.videoList(t)
        };
        let e = document.querySelectorAll('[data-widget="video-list"]');
        Array.prototype.map.call(e, (function(i) {
            i = new t.videoList(i)
        }))
    }(PULSE.app, PULSE.ui, PULSE.app.common, PULSE.core);